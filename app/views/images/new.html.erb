<div class="h1 header">Add a new Image</div>

<%= form_for(:image, :url => {:action => 'create'}, :html => {:multipart => true}) do |f| %>
  <%= f.file_field :image %>
  <br>
  <%= f.submit "Upload" %>
<% end %>
